class A{
  let x = 0;
  let y = 0;
  foo(){
    return x + y;
  }
  bar(z){
    return x+y+z;
  }
}
main() {
  obj = new A;
  obj.x = 1;
  obj.y = 2;
  let m = obj.foo(); {
    return m + obj.bar(10);
  }
}